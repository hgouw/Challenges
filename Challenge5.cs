using System;
using System.Linq;

namespace Challenges
{
    /// <summary>
    /// Challenge 5
    /// Given an array of integers, return the distinct pairs of array values whose sum is an odd number.
    /// [1, 2, 3, 4, 2] returns [1, 2], [1, 4], [2, 3], [3, 4]
    /// </summary>
    public static class Challenge5
    {
        /*
        public static int[,] OddSum(int[] integers)
        {
            int[] array = new int[] { 1, 5, 7, 2, 9, 8, 4, 3, 6 }; // given array
            int sum = 10; // given sum
            for (int i = 0; i <= array.Count() - 1; i++)
                if (array.Contains(sum - array[i]))
                    Console.WriteLine("{0}, {1}", array[i], sum - array[i]);

        }
        */
    }
}
